{
  "name": "dawns-mahjong",
  "version": "0.0.0",
  "private": true,
  "description": "Mahjong solitaire built for Dawn — 90, Elvis fan, playing on a Samsung tablet.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-sw.mjs",
    "preview": "vite preview",
    "test": "playwright test",
    "verify:build": "npm run build && node scripts/verify-build.mjs",
    "assets:sync": "node scripts/sync-assets.mjs",
    "measure:tiles": "node scripts/measure-tile-sheet.mjs",
    "build:icons": "node scripts/build-icons.mjs",
    "check:layouts": "node scripts/check-layouts.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
